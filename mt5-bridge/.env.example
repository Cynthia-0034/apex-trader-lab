# Copy to .env on the VPS and fill in real values. Never commit the real .env.
MT5_BRIDGE_TOKEN=replace-with-a-long-random-string-min-48-chars
MT5_LOGIN=12345678
MT5_PASSWORD=your-broker-password
MT5_SERVER=ICMarketsSC-Demo
# Optional: full path to terminal64.exe if multiple MT5 installs exist
# MT5_PATH=C:\Program Files\MetaTrader 5\terminal64.exe
